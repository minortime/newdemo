<%= form_with(model: quote, local: true) do |form| %>
  <% if quote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(quote.errors.count, "error") %> prohibited this quote from being saved:</h2>

      <ul>
      <% quote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :author %>
    <%= form.text_field :author, id: :quote_author , class:"w3-input w3-border" %>
  </div>

  <div class="field">
    <%= form.label :quotation %>
    <%= form.text_field :quotation, id: :quote_quotation , class:"w3-input w3-border" %>
  </div>

  <div class="field">
    <%= form.label :source %>
    <%= form.text_field :source, id: :quote_source , class:"w3-input w3-border" %>
  </div>

  <div class="actions">
    <%= form.submit :Submit, class:"w3-button w3-red w3-round-xlarge" %>
  </div>
<% end %>
